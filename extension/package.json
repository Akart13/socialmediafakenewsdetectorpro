{
  "name": "social-media-fact-checker-extension",
  "version": "1.0.0",
  "description": "Chrome MV3 extension for fact-checking social media posts",
  "scripts": {
    "build": "esbuild src/popup.ts --bundle --outfile=popup.js --format=esm --target=es2020",
    "dev": "esbuild src/popup.ts --bundle --outfile=popup.js --format=esm --target=es2020 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
